# Jira Weekly Status Reporter

This project use to fetch issue statistics from Jira Cloud and distribute a summary report via email.

## 📁 Project Structure
* `jira-email-notification.py`: The main Python script for API data retrieval and email dispatch.
* `.env`: Configuration file for storing Jira API tokens and Gmail App Passwords.
* `requirements.txt`: List of required Python libraries (`requests` and `python-dotenv`).
* `README.md`: Setup and automation instructions.

## 🛠 Setup Instructions

### 1. Install Dependencies
Run the following command in your terminal to install the necessary libraries:
```bash
pip install -r requirements.txt# jira-weekly-reporter
